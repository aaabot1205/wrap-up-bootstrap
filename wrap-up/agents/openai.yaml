interface:
  display_name: "Wrap Up"
  short_description: "Reconcile docs; publish only when explicit"
  default_prompt: "Use $wrap-up to close and verify the current phase without publishing; add publish only when Git publication is intended."
