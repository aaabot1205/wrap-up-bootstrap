interface:
  display_name: "Bootstrap"
  short_description: "Load project context and identify the next action"
  default_prompt: "Use $bootstrap to understand this project and prepare to continue the work."
