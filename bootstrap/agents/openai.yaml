interface:
  display_name: "Bootstrap"
  short_description: "Load evidence-qualified project context"
  default_prompt: "Use $bootstrap to reconstruct evidence-qualified project context and identify the next action."
